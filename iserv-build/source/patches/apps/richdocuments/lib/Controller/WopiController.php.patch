diff --git apps/richdocuments/lib/Controller/WopiController.php apps/richdocuments/lib/Controller/WopiController.php
index 6575c3cd8..f8cd43b60 100644
--- apps/richdocuments/lib/Controller/WopiController.php
+++ apps/richdocuments/lib/Controller/WopiController.php
@@ -145,6 +145,7 @@ class WopiController extends Controller {
 
 		$userId = !$isPublic ? $wopi->getEditorUid() : $guestUserId;
 
+		$downloadDisabled = $wopi->getHideDownload() || $shouldUseSecureView || $this->config->getSystemValueBool('iserv_disable_file_downloads', false);
 
 		$response = [
 			'BaseFileName' => $file->getName(),
@@ -166,11 +167,11 @@ class WopiController extends Controller {
 			'EnableShare' => $file->isShareable() && !$isVersion && !$isPublic,
 			'HideUserList' => '',
 			'EnableOwnerTermination' => $wopi->getCanwrite() && !$isPublic,
-			'DisablePrint' => $wopi->getHideDownload() || $shouldUseSecureView,
-			'DisableExport' => $wopi->getHideDownload() || $shouldUseSecureView,
-			'DisableCopy' => $wopi->getHideDownload() || $shouldUseSecureView,
-			'HideExportOption' => $wopi->getHideDownload() || $shouldUseSecureView,
-			'HidePrintOption' => $wopi->getHideDownload() || $shouldUseSecureView,
+			'DisablePrint' => $downloadDisabled,
+			'DisableExport' => $downloadDisabled,
+			'DisableCopy' => $downloadDisabled,
+			'HideExportOption' => $downloadDisabled,
+			'HidePrintOption' => $downloadDisabled,
 			'DownloadAsPostMessage' => $wopi->getDirect(),
 			'SupportsLocks' => $this->lockManager->isLockProviderAvailable(),
 			'IsUserLocked' => $this->permissionManager->userIsFeatureLocked($wopi->getEditorUid()),

diff --git apps/viewer/src/views/Viewer.vue apps/viewer/src/views/Viewer.vue
index 21a00d1..35ec3da 100644
--- apps/viewer/src/views/Viewer.vue
+++ apps/viewer/src/views/Viewer.vue
@@ -76,7 +76,7 @@
 				@click="showSidebar">
 				{{ t('viewer', 'Open sidebar') }}
 			</NcActionButton>
-			<NcActionButton v-if="canDownload"
+			<NcActionButton v-if="enabledDownloads && canDownload"
 				:close-after-click="true"
 				@click="onDownload">
 				<template #icon>
@@ -432,6 +432,16 @@ export default defineComponent({
 				return false
 			}
 		},
+
+		/**
+		 * Configuration option to disable download buttons
+		 *
+		 * @return {boolean}
+		 */
+		enabledDownloads() {
+			return !OC.appConfig.core.disableFileDownloads;
+
+		},
 	},
 
 	watch: {
@@ -1198,6 +1208,12 @@ export default defineComponent({
 				return
 			}
 
+			// IServ: also block the keyboard shortcut (Ctrl+S) and any programmatic
+			// download path when downloads are globally disabled, not just the button.
+			if (!this.enabledDownloads) {
+				return
+			}
+
 			// Get the current handler for this file
 			const mime = this.currentFile.mime
 			const alias = mime?.split('/')[0]
